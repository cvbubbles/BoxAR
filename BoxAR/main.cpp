
#include"cmdstd.h"

/* - Fix the bug of excessive memory usage
- Update the rendering process
- Delete unrelated files
- Update the README.md
*/
int main()
{
	//exec("test_detectors");
	exec("test_ardetectors");
	//exec("tools.gen_box_model");
	//exec("tools.calib_camera");
	//int fourcc = CV_FOURCC('M', 'P', '4', '2');
	//VideoWriter writer3("output.avi", fourcc, 30.0, Size(640, 480), true);
	//for (int i = 0; i <= 2300; ++i) {
	//	char buf[1024];
	//	sprintf(buf, "D:\\datasets\\doll3\\03f\\%04d.png", i);
	//	cv::Mat img = cv::imread(buf);
	//	writer3 << img;
	//}
	//writer3.release();
	return 0;
}



 